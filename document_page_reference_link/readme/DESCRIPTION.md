This module simplifies the link between document pages using the reference
field provided by `document_page_reference`.

Pages that contain elements like {{XXX}}, where XXX is the reference of
another page, are rendered with a direct link to that page, and the marker
is parsed as the linked page display name.
